{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Extension",
      "preLaunchTask": "compileYmlToJson",
      "type": "extensionHost",
      "request": "launch",
      "runtimeExecutable": "${execPath}",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}"
      ]
    }
  ]
}
